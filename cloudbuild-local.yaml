steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-f', 'Dockerfile.cloudrun', '-t', 'gcr.io/$PROJECT_ID/podcast-web:deployed-now', '.']
images:
- 'gcr.io/$PROJECT_ID/podcast-web:deployed-now'